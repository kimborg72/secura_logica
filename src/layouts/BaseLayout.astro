---
import { ClientRouter } from 'astro:transitions';
import BaseHead from '@components/seo/BaseHead.astro';
import type { Locale } from '@i18n/types';
import '@styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  locale?: Locale;
  ogType?: 'website' | 'article';
  svOnly?: boolean;
}

const { title, description, ogImage, locale = 'sv', ogType, svOnly } = Astro.props;
---

<!doctype html>
<html lang={locale}>
  <head>
    <BaseHead title={title} description={description} ogImage={ogImage} locale={locale} ogType={ogType} svOnly={svOnly} />
    <ClientRouter />
  </head>
  <body class="min-h-screen">
    <slot />
  </body>
</html>
