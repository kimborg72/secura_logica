---
interface Props {
  schema: Record<string, unknown> | Record<string, unknown>[];
}

const { schema } = Astro.props;
const nonce = Astro.locals.cspNonce;
---

<script type="application/ld+json" nonce={nonce} set:html={JSON.stringify(schema)} />
