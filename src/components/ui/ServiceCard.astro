---
interface Props {
  title: string;
  description: string;
  href: string;
}

const { title, description, href } = Astro.props;
---

<a
  href={href}
  class="group block p-6 bg-white rounded-xl border border-border border-l-4 border-l-transparent hover:border-l-accent hover:border-accent-dark/30 hover:shadow-lg transition-all duration-300"
>
  <h3 class="text-lg font-semibold text-navy mb-1.5 group-hover:text-accent-dark transition-colors">
    {title}
  </h3>
  <p class="text-sm text-gray leading-relaxed">{description}</p>
  <span class="inline-flex items-center gap-1 mt-3 text-sm font-medium text-accent-dark opacity-0 translate-x-[-8px] group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-300">
    Läs mer
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
    </svg>
  </span>
</a>
