---
import { cn } from '@utils/cn';

interface Props {
  variant?: 'primary' | 'secondary' | 'ghost' | 'white';
  href?: string;
  size?: 'sm' | 'md' | 'lg';
  class?: string;
  [key: string]: any;
}

const { variant = 'primary', href, size = 'md', class: className, ...rest } = Astro.props;

const baseClasses = 'inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200';

const variants = {
  primary: 'bg-accent text-dark font-semibold hover:bg-accent-dark active:bg-accent-dark',
  secondary: 'border-2 border-dark text-dark hover:bg-dark hover:text-white',
  ghost: 'text-dark hover:text-accent-dark underline underline-offset-4 decoration-dark/30 hover:decoration-dark',
  white: 'bg-white text-dark hover:bg-white/90',
};

const sizes = {
  sm: 'px-4 py-2 text-sm',
  md: 'px-6 py-3 text-sm',
  lg: 'px-8 py-4 text-base',
};

const classes = cn(baseClasses, variants[variant], sizes[size], className);
const Tag = href ? 'a' : 'button';
---

<Tag href={href} class={classes} {...rest}>
  <slot />
</Tag>
